name = "Luna-Flow/type_theory"

version = "0.2.0"

import {
  "moonbitlang/quickcheck@0.14.0",
}

readme = "README.md"

repository = "https://github.com/Luna-Flow/type_theory"

license = "Apache-2.0"

keywords = [ "type-theory", "substitution", "rewriting", "semantics" ]

description = "A formal semantic substrate for Luna-Flow symbolic computation, with binding, substitution, rewriting, evaluation strategies, and lambda/type-theoretic cores."

options(
  source: "src",
)
